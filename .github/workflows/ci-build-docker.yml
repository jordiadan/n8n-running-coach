name: Build check (Dockerfile)

on:
  pull_request:
    paths:
      - 'Dockerfile'
      - 'fly.toml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Docker build
        run: |
          docker build -t n8n-ci:test .
